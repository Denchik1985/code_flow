name: code_flow
description: Free FlutterFlow Killer - Visual Flutter Editor
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.1
  flutter_colorpicker: ^1.0.3
  file_picker: ^5.3.3
  archive: ^3.3.7
  path: ^1.8.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.4.7

flutter:
  uses-material-design: true